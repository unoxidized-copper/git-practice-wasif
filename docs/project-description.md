# Project Description

**Basic Calculator** is a simple Python command-line application that takes two numbers as input from the user and performs basic arithmetic operations.

## Features

- Addition
- Subtraction
- Multiplication

## Project Structure

- `src/main.py` — Entry point, takes user input and displays results
- `src/utils.py` — Contains arithmetic functions (`add`, `subtract`, `multiply`)
